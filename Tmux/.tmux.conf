unbind C-b
set -g prefix M-a

bind M-Left select-pane -L
bind M-Right select-pane -R
bind M-Up select-pane -U
bind M-Down select-pane -D

bind h select-pane -L
bind l select-pane -R
bind k select-pane -U
bind j select-pane -D

bind Right split-window -h
bind Down split-window -v

bind v split-window -h
bind b split-window -v

unbind '"'
unbind %

bind Q kill-window
bind q kill-pane
unbind &
unbind x

# make mouse usable
set -g mouse on
set-window-option -g mode-keys vi

# set status bar
# set -g status-bg black
# set -g status-fg cyan
# set -g status-justify left
# set -g status-interval 2

# panes
set -g pane-border-style fg=cyan
set -g pane-active-border-style fg=cyan
set -g pane-active-border-style bg=default

# remove status panel
set -g status off
# set -g status-position bot

# source "/home/nathaniel/Documents/Programs/anaconda3/lib/python3.5/site-packages/powerline/bindings/tmux/powerline.conf"
# set -g default-terminal screen-256color
